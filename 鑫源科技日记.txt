                                                         2019年6月24入职鑫源科技

1.localStorage.setItem 存储必须是字符串格式，如果要存储的是js对象格式，必须先JSON.stringify转换为字符串
  localStorage.getItem  获取的是字符串，如果是对象，必须JSON.parse转换为JS对象，才能操作对象
2.获取本地ip，cmd ipconfig  IPv4 地址  关闭防火墙，共享本地项目
3.出生年日月转换成年龄，首先获取接口数据的时候：1。定义一个空数组，然后遍历接口数据，把方法return变成一个值。
4.点解列表时获取的id，无法传到弹出层的分班按钮，此时在data定义一个变量，然后当点击列表的时候把id保存在变量里面，要获取的时候直接this.data
5.下载二维码插件。使用时出现多次点击时生成多张二维码，document.getElementById("qrcode").innerHTML = "";一定要注意的是顺序，在JS从上到下的原则
6.点击审核时候 通过路由参数传id，利用vuex ，当点击的时候触发store里面的方法，把id作为传参，每点击一次就自动存储在localStorage，轻松获取epid
7.页面变化时无法重新加载获取列表的方法，一开始尝试watch监听$router，虽然可以，但是每次都会获取接口，太消耗内存了，然后想到了keep-alive中的
生命钩子，activated每次回到当前首页时会触发，这就达到了，每次都能刷新数据通过localStorage获取的epid，完美解决的 动态id的困扰！
8.iview表单验证的坑超级多，特别时数字验证的时候，明明有值却报错，在百度上很多案例。

9：与后端交互时出现很多问题
1.不会看接口文档，导致很多字段不是很熟悉，在交流上存在很大问题，经常传错值 不会传字段等问题
2.必传分页page常用的字段，还有最重要的一点是epid一定要放在params{}对象里面传，弄了很久，第一次遇到，b